<?xml version="1.0" encoding="UTF-8"?>
<blackBoxStochModel xmlns="http://www.openda.org"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.openda.org http://schemas.openda.org/blackBoxStochModelConfig.xsd">

    <modelConfig>
        <file>./simstratBlackBoxModel_sim.xml</file>
    </modelConfig>

    <vectorSpecification>

        <!--
            No state vector: Simstrat manages its own snapshot internally.
            The Simulation algorithm does not update or inject state.
        -->

        <!--
            Predictor: temperature at 0.5 m depth (obs space).
            Written by run_simstrat_sim.py and read by OpenDA for comparison
            against the stochObserver (noosObservations.xml).
        -->
        <predictor>
            <vector id="depth_0.5.temperature"/>
        </predictor>

    </vectorSpecification>

</blackBoxStochModel>
